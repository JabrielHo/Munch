# Cloudflare Pages SPA fallback.
# Serve index.html for every non-asset route so React Router deep links like
# /r/ABC123 (the shareable room link) resolve on a hard refresh. Static assets
# are served directly; the API lives on Convex (a separate origin), so nothing
# here shadows it.
/*    /index.html   200
